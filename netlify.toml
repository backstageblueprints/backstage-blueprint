# Netlify configuration — Backstage Blueprint platform

[build]
  # The site files live at the repo root for now.
  publish = "."

[functions]
  # Server-side code (Stripe checkout, webhook, content gate) will live here.
  directory = "netlify/functions"
